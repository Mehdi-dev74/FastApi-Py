# My FastAPI App 🚀

## Quick Start
```bash
# Clone & Install
git clone <your-repo>
cd my-fastapi-app
pip install -r requirements.txt

# Dev Server
uvicorn app.main:app --reload

# Docker
docker-compose up --build
